'use strict';

angular.module('saturnApp').factory('editContactService', editContactService);

editContactService.$inject = [];

/* @ngInject */
function editContactService() {
  var factory = {};
  return factory;
}
